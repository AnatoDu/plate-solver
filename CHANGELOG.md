# Changelog

Формат — по [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/);
версии — [SemVer](https://semver.org/lang/ru/). Числа регресса защищены
тест-воротами (`pytest`); эталонные отчёты — под хеш-контролем.

## [Unreleased]

## [0.6.6] — 2026-07-24

Функциональный минор «напряжения, данные, физика» — три волны к видению
полноценного комплекса: (1) полное напряжённое состояние до пользователя
(N с фиксом тихой потери, мембранная часть в лицевых σ, σ_vm, вторая
пластина пары) и самостоятельная работа с данными (документированная схема
fields.npz, plate-replot, VTK из case-файла, все собственные формы);
(2) перерезывающие Qx/Qy с честной характеристикой точности, профили сечений
и наложения (plate-profile + CSV), преднапряжённая собственная задача из
case-файла; (3) упругое основание Винклера во всех трактах (MMS до машинной
точности). Все изменения АДДИТИВНЫ: эталонные отчёты и SHA-хеши не тронуты.

### Добавлено
- **Пакет «напряжения и данные» (волна 1 к видению полноценного комплекса)**:
  - **мембранные усилия `N` доведены до пользователя** — исправлен тихий баг
    несовпадения сигнатур в `export.forces_on_grid` (N считались и ТЕРЯЛИСЬ;
    TypeError глотался); N восстановимы и ПОСЛЕ нелинейного контакта (мембрана
    сошедшегося состояния — `NonlinearContactResult.cu/cv`); N — в `fields.npz`,
    на новой фигуре `membrane` и в VTK;
  - **полные лицевые напряжения**: `ktn.stresses_faces` принимает `Nx, Ny, Nxy`
    — мембранная часть `T/h` входит в σ обеих лицевых (формула (11) при
    `T_ij ≠ 0`; для линейных теорий N ≡ 0 — прежние числа бит-точно);
  - **эквивалентное фон Мизеса** `ktn.von_mises`: `svm_top/svm_bot` в npz
    (и для второй пластины пары), новая фигура `von_mises`;
  - **фигура `stress_faces2`** — напряжения ВТОРОЙ пластины пары (данные
    сохранялись со схемы 2, фигуры не было);
  - **`fields.npz` схема 3** с ПОЛНОЙ документацией ключей
    (docs/CASE_SCHEMA.md#output) — самостоятельные графики без чтения кода;
  - **все собственные формы** в npz (`eigen_modes (n, g, g)`, `eigen_values`)
    — раньше сохранялась только первая;
  - **CLI `plate-replot <dir>`** — перерисовка фигур из fields.npz без
    пересчёта; **ключ `[output] vtk = true`** — result.vtk для ParaView;
  - **полная история сходимости МОР** (`residual_history`) в result.json.
- **Перерезывающие силы `Qx, Qy`** (`export.shear_forces_on_grid`) — из
  РАВНОВЕСИЯ моментов `Qx = ∂Mx/∂x + ∂Mxy/∂y` (конечные разности фоновой
  сетки); в `fields.npz` и на новой фигуре `shear`. Точность честно
  задокументирована: квадратурный пол маски ~1/Q, усиленный производными
  (медиана ~1.4 % при Q=200, убывает с Q — проверено 200→1000). Верификация —
  ТОЧНОЕ осесимметричное равновесие `Q_r = q·r/2` (не зависит от КУ) и
  локальное равновесие `div Q = −q` на прямоугольнике (медиана < 0.1 %).
- **Профили сечений и наложения** (`viz.section_profile`,
  `viz.overlay_profiles`; CLI `plate-profile`): профиль ЛЮБОГО сеточного поля
  вдоль произвольного отрезка из fields.npz (без пересчёта), CSV-экспорт;
  несколько каталогов — НАЛОЖЕНИЕ кривых (сравнение теорий/кейсов вдоль
  одного сечения).
- **Упругое основание Винклера** (`[model] winkler = k_w`): оператор
  `D·Δ²w + k_w·w = q`, свёрнут в изгибную жёсткость (+`k_w∫ψψ`) — работает во
  ВСЕХ трактах (classic clamped, нелинейные Карман/КТН обеих кромок и mixed,
  собственные задачи, контакт). Верификация: MMS до МАШИННОЙ точности
  (rel ~6·10⁻¹¹), `k_w = 0` бит-точно, асимптотика `k_w → ∞`, монотонное
  ужесточение. Классический мягкий шарнир (расщепление) с основанием
  несовместим — ясный отказ с советом (`theory = karman`).
- **Преднапряжённая собственная задача из case-файла**
  (`[eigen] prestress = true` + `[load]` + `theory = "karman"`): поле `N(w)`
  из кармановского решения под нагрузкой; частоты/устойчивость НАГРУЖЕННОЙ
  пластины штатным входом (раньше — только API). Ручные `Nx/Ny/Nxy` при
  prestress запрещены; несжимающее преднапряжение при buckling — ясный отказ.

### Исправлено
- `[eigen]` + нелинейная теория — честный отказ (раньше принималось и МОЛЧА
  игнорировалось: анализ всегда линейный Кирхгоф); колонка «контакт» в сводке
  `reproduce_all.py` определяется флагом `enabled`, а не наличием секции.

## [0.6.5] — 2026-07-24

Функциональный минор (снят задел v0.7): контакт пары пластин на МЯГКОМ ШАРНИРЕ
и СИЛОВОЕ управление парой (∫r = P), СМЕШАННЫЕ КУ для нелинейного Кармана
(линейный предел = ряд Леви машинно), точная пореберная полигонная квадратура
∂Ω (сертификация §3.5 у выпуклых углов; расходимость у входящего угла
локализована В СЛАБОЙ ФОРМЕ — NOTES §9), `contact.mor_anderson` как case-ключ.
Плюс аудит устойчивости новой поверхности (315 прогонов, адверсариальная
верификация): исправлен тихий шум замыкания силового штампа при Андерсоне и
молчаливое игнорирование `[plate2.bc] = mixed`; превентивные ограждения
(потолок окна Андерсона, free-стороны Кармана, nested-пара). Все изменения
АДДИТИВНЫ: эталонные отчёты и их SHA-хеши не тронуты.

### Добавлено
- **Силовое управление ПАРОЙ пластин** (снят задел v0.7): для нелинейной пары
  МОР+КТН ключ `contact.force` = суммарное усилие прижатия `P = ∫r`; ищется
  НАЧАЛЬНЫЙ зазор `z` (`z < 0` — натяг) ПРОДОЛЖЕНИЕМ по `z` с тёплым стартом
  соседним уровнем (`NonlinearTwoPlateMOR.set_gap` + тёплый `solve`; без этого
  каждый уровень — полный парный МОР с нуля, минуты вместо секунд) и brentq в
  найденном мосте. Несошедшийся внутренний МОР → понятный `CaseError` (не мусор
  в brentq). Рабочая область — умеренное прижатие (`z*` вблизи касания); глубокий
  натяг — предел совмещённой схемы (честный отказ с советом). Верификация:
  `∫r = P` (допуск ~1e-2); редукция — пара с жёсткой 2-й пластиной = одиночный
  силовой штамп (rel < 1e-2 по `w` и уровню). Классическая пара с `force`
  остаётся отклонённой. CI `ktn_full_two_plates_force`; `Result.force_total`/
  `Result.level` заполняются и для пары.
- **Точная пореберная полигонная квадратура ∂Ω** (`ktn_full._polygon_boundary_quad`;
  `Domain.polygon`, задаётся `make_L`): Гаусс на каждом ребре с точными нормалями
  (узлы сдвинуты внутрь на `1e-8·diam` — обход устранимых 0/0 производных
  R-функции ровно на линиях границы). Тождество Грина на L улучшено с 7.8e-2
  (contourpy) до ~2e-4; на прямоугольнике совпадает со звёздной квадратурой
  (rel < 1e-6) и **впервые сертифицирует** §3.5 у ВЫПУКЛЫХ углов (7 значащих цифр
  при измельчении n/edge 16 → 192). Итог диагноза для ВХОДЯЩЕГО угла (L):
  расходимость — В СЛАБОЙ ФОРМЕ (гессиан R-дизъюнкции `~1/d`, лог-расходимость),
  НЕ в квадратуре ⇒ `ktn_full + soft_hinge` на L/compose остаётся закрытым
  валидатором (NOTES §9; корректный путь — сингулярное обогащение, v0.7+).
- **Смешанные КУ для геометрически-нелинейной теории Кармана** (снят задел v0.7):
  структура прогиба `w = ∏(сторона)^{2|1|0}·Φ` (`clamped`→фактор², `hinge`→фактор¹,
  `free`→без множителя) обобщает защемление/шарнир на СМЕШАННЫЕ кромки прямоугольника
  поверх мембранной связи Фёппля–Кармана (`_w_structure_mixed`, `KarmanPlate(bc_type=
  "mixed", sides=…)`). Верификация: ЛИНЕЙНЫЙ предел (малая нагрузка) совпал с
  КЛАССИЧЕСКИМ смешанным решателем (ряд Леви) МАШИННО (rel 2·10⁻¹¹); нелинейный
  режим показывает мембранное ужесточение (Карман < классика); редукция к
  clamped/soft_hinge на уровне дискретизации. CI `karman_rect_mixed`,
  `tests/test_mixed_karman.py`. `ktn_full` со смешанными КУ (граничный член §3.5 на
  шарнирных сторонах) — v0.7+.
- **Контакт ПАРЫ пластин на мягком шарнире** (снят задел v0.7): нелинейная пара
  МОР+КТН (`NonlinearTwoPlateMOR`) теперь работает на кромке `soft_hinge` (звёздная
  область; L/compose с входящим углом отклоняется). `_plate2_solver` уважает
  `[plate2.bc]` (был хардкод `clamped`). Редукция — пара с жёсткой 2-й пластиной →
  одиночный контакт (rel 1.9·10⁻⁴). CI `ktn_full_two_plates_soft_hinge`.
  Силовое управление парой — см. отдельный пункт выше (реализовано тёплым
  стартом продолжения по z).
- **`contact.mor_anderson` — case-файловый ключ** (завершает ускорение Андерсона
  из 0.6.4): окно проекц. Андерсона внешнего цикла МОР теперь задаётся в `[contact]`
  (0 — выкл.); применён и к парному МОР (`NonlinearTwoPlateMOR`).

### Исправлено
- **Аудит устойчивости v0.6.5** (315 прогонов новой поверхности + смежной старой,
  адверсариальная верификация): (1) силовой штамп + `mor_anderson` — смешение
  Андерсона делало останов МОР полусошедшимся квазислучайно, скаляр
  `F(level) = ∫r − P` шумел на ~7–11 % и brentq садился мимо цели МОЛЧА;
  теперь в силовом режиме одиночного штампа Андерсон принудительно отключается
  (предупреждение; замыкание ∫r = P восстановлено до машинного), в парном
  силовом (где Андерсон необходим) — контроль фактического замыкания
  с честным предупреждением при отклонении > 2 %. (2) `[plate2.bc] = mixed`
  для классической пары принимался и МОЛЧА решался как soft_hinge
  (sides игнорировались) — теперь ясный отказ. (3) `mor_anderson > 20` —
  отказ (большие окна вырождают МНК и разваливают итерацию: окно 50
  расходится там, где 0–20 сходятся). (4) FREE-стороны для `karman + mixed` —
  отказ (не верифицированы; глубоко-нелинейный режим free+movable не сходится
  Пикаром; для free — `theory = classic`). (5) `scheme = "nested"` для пары —
  отказ (пара реализована только совмещённым циклом; тихо игнорировался
  с v0.6.3). (6) misuse-ошибка нелинейных contact-ключей анкерована на реально
  заданный ключ. Плюс актуализация доков (пара на шарнире/силовая в §contact,
  эллипс-ремарка, счётчик ноутбуков).

## [0.6.4] — 2026-07-23

Функциональный минор: собственные задачи (устойчивость/колебания, в т.ч.
преднапряжённые частоты нагруженной пластины), Ньютон-ускорение нелинейной
итерации (сквозной вход) и ускорение Андерсона внешнего цикла МОР, замыкание
редукционной лестницы R1–R5 воротами R3 (независимый 1D↔2D радиальный решатель
полной КТН, обе кромки), MMS-сертификат полной КТН при фиксированных усилиях,
сертифицированный контакт на защемлении, эллипс-примитив. Плюс матричный аудит
устойчивости (~950 прогонов) с исправлением вырожденной compose-области и
fail-fast валидацией эталонов. Все изменения АДДИТИВНЫ: эталонные отчёты и их
SHA-хеши (results/reference/reference_v0.6.md) не тронуты.

### Добавлено
- **Ускорение Андерсона внешнего цикла МОР** (нелинейный контакт, `mor_anderson`):
  проекционный Андерсон по реакции `r` над проекционным фиксированным шагом
  `r ← max(0, r + β_eff·(u_c − Δ))` — смешивает историю невязок, проецирует
  `r ≥ 0` ПОСЛЕ смешения, сходится по истинной невязке неподвижной точки
  `‖F(r)−r‖`. На ВЛОЖЕННОЙ (эталонной) схеме, где каждый внешний шаг — ПОЛНЫЙ
  нелинейный КТН-решатель, окно 3–5 даёт **8–9× меньше внешних шагов** при ТОЙ ЖЕ
  неподвижной точке (rel ~4·10⁻⁵). Опция `Config.mor_anderson` (0 — выключено,
  поведение по умолчанию число-в-число). `tests/test_contact_nonlinear.py`.
- **Ворота R3 — 1D↔2D для полной КТН** (B4): независимый осесимметричный
  радиальный решатель `radial_ktn.RadialKTN` (иная дискретизация — радиальный
  Ритц по `r`, своя квадратура, свой мембранный подшаг) воспроизводит 2D-решение
  `KTNPlate` на круге при равномерной нагрузке. Замыкает редукционную лестницу
  (R1, R2, **R3**, R4, R5): у полной нелинейной КТН нет литературных эталонов, а
  R3 — САМЫЙ сильный сертификат, ибо проверяет ПОЛНУЮ живую связь `N(w)` (сильнее
  MMS с замороженным `N`, B6). Точная редукция к оси:
  `D Δ²w − (I−h_ψ²Δ)L(w) = q`, `L = N_r w'' + N_θ w'/r`, `N` из осесимметричной
  плоской задачи Фёппля–Кармана. Согласие 1D↔2D дискретизационно-ограничено
  (rel(w_max) ≤ ~4·10⁻³ на быстрой сетке, УБЫВАЕТ с ростом 2D-`p`:
  `4.1e-3 → 3.0e-3 → 2.5e-3 → 2.0e-3` при `p = 12,14,16,18`), а КТН/Карман
  отношение совпадает 1D↔2D до ~3·10⁻⁵ — редукция верна. `tests/test_ktn_full.py`
  (Gate R3, быстрые + `big` сходимость). **Обе кромки** (добор): R3 расширен на
  мягкий шарнир (структура `(a²−r²)Φ`, натуральное `M_r=0`; граничный член §3.5 —
  в осесимметрии вырожден/машинно нулевой, отдельно сверен тождеством Грина);
  совпадение 1D↔2D ~6·10⁻⁴, 2D-сторона ускорена Ньютоном.
- **Сертифицированный контакт — защемление** (B5): осесимметричный эталон
  контакта Кирхгофа (`analytic_auto.axisym_contact_solution`) получил параметр
  `bc` — кромка `soft_hinge` (`w=Δw=0`) ИЛИ `clamped` (`w=w'=0`,
  `w_free(0)=q₀a⁴/64D`, круг жёстче, зазор меньше). Резолвер (`references.py`)
  принимает контактный `analytic` для `classic` × `(soft_hinge|clamped)`;
  совпадение с решателем rel ~3·10⁻³. CI-кейс `circle_clamped_contact_certified`;
  `tests/test_contact_certified.py`.
  *Отложено:* `ktn_linear` контакт эталона не получил — КТН-поправка входит в
  лицевое условие Синьорини (реакция гонится прогибом нижней лицевой), так что
  базовое поле отклоняется от кирхгофовского с ростом толщины; классический
  эталон был бы ложно-положителен лишь в тонком пределе (найдено адверсариальной
  проверкой). Корректный `ktn_linear`-эталон требует теории-специфичной
  аналитики.
- **MMS-сертификат полной КТН при замороженных усилиях** (B6, fixed-N): заморозка
  мембранных `N̄` линеаризует оператор КТН, и по изготовленному `w` строится
  нагрузка, обращающая сильную форму `D Δ²w − (I−h_ψ²Δ)L(w) = (I−h_*²Δ)q`
  (`ladder.mms_ktn_load_and_exact`; экранированный Пуассон обращается ТОЧНО —
  ряд Неймана обрывается на полиномиальном `q`). Линейный решатель
  `KTNPlate.solve_fixed_forces` на ТОЙ ЖЕ дискретизации воспроизводит `w_MMS` —
  единый сертификат ВСЕХ членов сборки: изгиб, геом. жёсткость, член B (`h_ψ²`),
  член A (`h_*²`). Прямоугольник (полиномиальная ω) ⇒ машинная точность (~1e-13);
  круг ⇒ ~1/Q. Положительно-отрицательные проверки: рассогласованная нагрузка
  (без члена B / без Δq) НЕ воспроизводит решение — члены доказаны и присутствием,
  и правильностью. Подключён к резолверу (`reference="mms"` + `theory="ktn_full"`);
  CI-кейс `ktn_full_rect_mms`; `tests/test_mms_ktn.py`, `tests/test_mms_reference.py`.
- **Ньютон-ускорение нелинейной итерации** (Карман/КТН) — согласованный
  касательный оператор `J = dR/dc` (аналитический, `_newton_tangent`) с защитой
  ЛИНЕЙНЫМ ПОИСКОМ (backtracking) даёт КВАДРАТИЧНУЮ сходимость: ~5–7 итераций
  против десятков/сотен у Пикара, слабая зависимость от уровня нагрузки. Для КТН
  касательная включает вариацию регуляризатора `h_ψ²(I−h_ψ²Δ)L` и (на мягком
  шарнире) граничного члена §3.5. Включение — ключ `[model] karman_method` /
  `ktn_method = "newton"` (по умолчанию `"picard"` — числа существующих
  постановок не тронуты). Касательная сверена КОНЕЧНЫМИ РАЗНОСТЯМИ (~1e-11) и
  даёт то же решение, что Пикар; там, где Пикар не сходится за 300 итераций
  (тонкая пластина `h=0.3`), Ньютон сходится за 6. `tests/test_newton.py`.
  **Сквозной вход** (v0.6.4, добор): `karman_method`/`ktn_method = "newton"`
  маршрутизируется через диспетчер (case-файл → Ньютон); CI-кейсы
  `karman_circle_clamped_newton`, `ktn_full_circle_clamped_newton`; тест
  диспетчер-эквивалентности Ньютон = Пикар (обе теории).
- **Собственные задачи: устойчивость и колебания** (`eigenmodes.py`) — новый тип
  анализа поверх ЛИНЕЙНОЙ сборки структурного Ритца (те же `S_bend`/`K_geo` +
  матрица масс `∫ψψ`). `buckling(plate, Nx, Ny, Nxy)` — критические множители
  потери устойчивости `(K + λ K_geo(N⁰))φ = 0` (формы выпучивания);
  `natural_frequencies(plate, rho_h)` — собственные частоты `Kφ = ω²Mφ` (формы
  колебаний). Обобщённые собственные значения через `scipy.linalg.eig` (терпит
  вырожденную правую матрицу — избыточность базиса Ритца). ВЕРИФИКАЦИЯ против
  классических эталонов Кирхгофа (есть в литературе!): квадрат SSSS `k=4.0001`,
  CCCC `k=10.07`; круг CCCC `N_cr·a²/D=14.73` (теория 14.68); частоты SSSS точны,
  CCCC `λ1=35.99`, круг `λ1=10.26` (теория 10.216). `tests/test_eigenmodes.py`,
  фасад `plate_solver.buckling`/`natural_frequencies`/`linear_plate`.
  **Преднапряжённые собственные задачи** (v0.6.4, добор): `natural_frequencies` и
  `buckling` приняли параметр `prestress` — поле усилий `N(w)` из РЕАЛЬНОГО
  нелинейного решения (результат Кармана/КТН) либо своё `(Nx,Ny,Nxy)`.
  `K_eff = K + K_geo(N)`: растяжение от поперечной нагрузки УЖЕСТОЧАЕТ пластину и
  ПОВЫШАЕТ частоты (новая физика — колебания/устойчивость НАГРУЖЕННОЙ пластины).
  Верификация: SSSS-квадрат под двухосным натяжением совпал с точной формулой
  `λ = √(4π⁴ + 2N₀π²a²/D)` (rel < 1e-3); частота монотонно растёт с нагрузкой
  через `N(w)`; нулевое преднапряжение бит-точно = ненапряжённому.
- **Секция `[eigen]` в case-файле** — устойчивость/колебания через
  `plate-solve case.toml` (альтернатива изгибу/контакту; `[load]`/`[contact]` не
  нужны). CLI печатает таблицу значений, `--figures` рисует формы (`_modes.png`);
  `Result.eigen` несёт `EigenPair`. `viz.plot_modes` — сетка форм. CI-кейсы
  `eigen_vibration_square`, `eigen_buckling_circle`.

### Исправлено
- **Вырожденная compose-область — понятный отказ вместо падения** (аудит
  устойчивости v0.6.4, ~950 прогонов): compose-дерево с ПУСТОЙ внутренностью
  (`difference` с поглощением уменьшаемого; `intersect` непересекающихся
  примитивов) структурный валидатор пропускал, а сборка падала делением на ноль
  и ошибкой LAPACK на NaN. Теперь `dispatch.build_domain` ловит это ясной
  `CaseError` («ПУСТАЯ область» / «вырожденная область»). `tests/test_problem.py`.
  Полный матричный аудит (изгиб 326, контакт 128, собств. задачи, некорректный
  ввод 157, API 211) — иных падений/нарушений ограничений не выявил.
- **Эталон верификации — валидация fail-fast** (полировка по аудиту): применимость
  `verify.reference` теперь проверяется при РАЗБОРЕ case-файла (`_validate_cross`),
  а не лениво в `resolve_reference`. `analytic` под контактом вне
  `circle+(soft_hinge|clamped)+classic+основание` и `mms` вне `clamped+rectangle|circle`
  отклоняются понятной `CaseError` СРАЗУ (переиспользован предикат
  `_is_axisym_contact_case`, без дублирования правил).
- **`buckling` при несжимающем поле — ясное сообщение** вместо пустого результата:
  чистое растяжение/ноль ⇒ терять устойчивость нечему; функция бросает `ValueError`
  с подсказкой задать сжатие (было — молча пустой `values`).

## [0.6.3] — 2026-07-22

Функциональный минор: КАЖДАЯ из четырёх теорий (`classic`, `karman`,
`ktn_linear`, `ktn_full`) теперь решает и ИЗГИБ, и КОНТАКТ через штатный вход
(case-файл → `plate-solve`). Ранее реализованный и верифицированный метод
нелинейного контакта МОР+КТН (`contact_nl.py`) был доступен только на уровне
API — теперь он подключён к диспетчеру. Изменения СТРОГО аддитивны: числа
существующих постановок (`classic`/`karman`/`ktn_linear`/`ktn_full` изгиб,
`classic`/`ktn_linear` контакт) не тронуты — эталонные отчёты и их SHA-хеши
остались прежними.

### Добавлено
- **Полная КТН на мягком шарнире на `annulus`** (МНОГОСВЯЗНАЯ область): общая
  КОНТУРНАЯ квадратура ∂Ω (`_contour_boundary_quad` через contourpy — ловит оба
  контура `ω = 0`), диспетчер `_boundary_quad` (звёздная для circle/ellipse/
  rectangle, контурная для кольца). Тождество Грина на кольце rel ~1e-3. Изгиб
  и контакт. Область с ВХОДЯЩИМ углом (L/compose) — v0.7 (реентрантная особенность).
- **Силовое управление нелинейным контактом** (`contact.force` при
  `theory = karman | ktn_full`, одиночная пластина): `F(level) = ∫r − P = 0`
  через brentq поверх `NonlinearContactMOR`; силовое уравнение замыкается до
  ~1e-9. Пара с force — v0.7.
- **Абсолютный эталон контактной задачи** (`verify.reference = "analytic"` для
  круг + мягкий шарнир + classic + основание): подключено сертифицированное
  осесимметричное решение Кирхгофа (`axisym_contact_solution` — центральная
  плоская зона + кольцевая реакция). Гейт — прогиб ВНЕ контактной зоны
  (`w_max = Δ` тривиален), rel ~5e-3. Контакт круга теперь верифицируется
  АБСОЛЮТНЫМ эталоном, а не только инвариантами (снят устаревший запрет v0.2).
  Прочий контакт — по-прежнему ворота инвариантов. `cases/ci/circle_soft_contact_certified.toml`.
- **Полная КТН на мягком шарнире на `rectangle`** (в дополнение к circle/ellipse):
  звёздная квадратура ∂Ω точна и на прямоугольнике (углы — множество меры нуль;
  тождество Грина держится), изгиб и контакт.
- **Карты обжатия и моментов в matplotlib** (`viz.replot`): карта
  `dh = w_нижн.лицевая − w_срединная` (только КТН, `dh ≠ 0`) и карты изгибных
  моментов `Mx, My, Mxy` (ранее только в VTK). Файлы `compression.png`,
  `moments.png` при `--figures`.
- **Полная КТН на мягком шарнире** (`theory = ktn_full`, `bc = soft_hinge`;
  circle | ellipse | rectangle) — изгиб И контакт через case-файл. Шарнир отличается от
  защемления одним граничным членом слабой формы `−h_ψ²∮ L·∂ψ/∂n ds` (формула
  Грина при `v = 0` на ∂Ω; для защемления `∂ψ/∂n = 0` ⇒ член ноль), собираемым
  звёздной квадратурой по ∂Ω (`_star_boundary_quad`). ВЕРИФИКАЦИЯ: граничный
  член подтверждён ДИСКРЕТНЫМ ТОЖДЕСТВОМ ГРИНА (`∫ v ΔL = ∫ L Δv − ∮ L ∂ψ/∂n` до
  точности квадратуры; без члена — 50 % ошибки), редукциями (члены-выкл →
  Карман бит-точно; `h → 0` → Кирхгоф), сходимостью по `p` и стыковкой с
  теориями (шарнир податливее защемления; морфинг Карман↔КТН). Это ЗАКРЫВАЕТ
  последнюю клетку сетки: каждая теория решает прогиб И контакт на защемлении И
  шарнире. Уровень доверия — как у защемлённой КТН (редукции + внутренняя
  согласованность; независимый литэталон §R3 отложен для обоих). Ограничения:
  circle | ellipse (звёздная квадратура ∂Ω), одиночная пластина, равномерная
  нагрузка. См. `tests/test_soft_hinge_ktn.py`, `cases/ci/ellipse_soft_hinge_*`.
- **Примитив геометрии `ellipse`** (полуоси `a`, `b`; при `a = b` — круг):
  форма произвольного очертания прямо в case-файле (`geometry.make_ellipse`,
  `ellipse_expr`). Изгиб и контакт на эллипсе для линейных теорий — при
  `clamped` И `soft_hinge`; полная КТН и нелинейный контакт — при `clamped`.
  Аналитического эталона нет (`reference = none | fem`). Не примитив `compose`
  (ограда compose-языка неизменна).
- **Нелинейный контакт через диспетчер** (`theory = karman | ktn_full` +
  `contact.enabled`): маршрутизация в `contact_nl.NonlinearContactMOR` —
  метод обобщённой реакции ВОКРУГ полного нелинейного решателя КТН. Условие
  непроникания на ЛИЦЕВОЙ поверхности (Синьорини на грани,
  `u_c = w + (h_c²−h_*²)Δw`): у `karman` коэффициент кривизны ноль (контакт по
  срединной), у `ktn_full` — физическая лицевая кривизна (подпись КТН).
- **Нелинейная пара пластин** (`target = plate2`, обе — одна нелинейная теория):
  `contact_nl.NonlinearTwoPlateMOR`, условие `u_c1 − u_c2 ≤ z` (§9.2).
- **Ключи схемы** `contact.scheme` (`nested | merged`, §4.2) и `contact.gain`
  (`secant | linear`, нормировка усиления оператора, теорема 4).
- **CI-случаи** нелинейного контакта: `karman_circle_clamped_contact`,
  `ktn_full_circle_clamped_contact`, `ktn_full_two_plates_contact` (`cases/ci/`,
  `reference = "none"`, ворота-инварианты).
- **Ворота диспетчера** `tests/test_contact_nonlinear.py`: инварианты (r ≥ 0,
  комплементарность, зона), редукция R1 (зазор → ∞ ⇒ r ≡ 0), лицевая подпись
  КТН (dh ≠ 0 у `ktn_full`, dh ≡ 0 у `karman`), редукция пары (жёсткая вторая
  пластина ⇒ опора), отказ несовместимых постановок.

### Исправлено
- **Ловушка `ktn_full` + `soft_hinge`**: комбинация давала рантайм-
  `NotImplementedError`; теперь это ПОДДЕРЖАННАЯ постановка (circle | ellipse,
  см. «Добавлено»), а для незвёздных областей — понятный `CaseError` (звёздная
  квадратура ∂Ω реализована для circle | ellipse).
- **Напряжения ВТОРОЙ пластины пары в `fields.npz`**: для КТН-решателей моменты
  теперь берутся через `moments_at` (верная степень структуры `ω²Φ`), а не через
  `bending_moments_full` с неверной степенью `ω¹`.
- **Документация**: снята устаревшая строка «контакт только при мягком шарнире»
  (v0.2) — классический контакт работает и при `clamped`; актуализированы
  docstring диспетчера и `docs/CASE_SCHEMA.md` (секции contact/plate2/model,
  таблица несовместимостей).

### Направления развития (v0.7)
- Полная КТН на мягком шарнире для областей с ВХОДЯЩИМ углом (L / compose с
  вырезом — реентрантная особенность рвёт точность граничной квадратуры);
  soft_hinge для КТН-пары; неравномерная (gaussian) нагрузка для нелинейного
  контакта (член КТН `−h_*²Δq` взаимодействует с негладкой реакцией); смешанные
  и `ktn_linear` пары; силовое управление парой; независимый литературный
  эталон полной КТН (gate R3, 1D↔2D) — общий для защемления и шарнира.
- Контакт при смешанных КУ; силовое управление нелинейным контактом и парой;
  смешанные и `ktn_linear` пары; независимая верификация КТН (gate R3, 1D↔2D).

## [0.6.2] — 2026-07-09

Патч сопровождения: согласование метаданных цитирования/архивации, ревизия
документации и обучающих ноутбуков к минту Zenodo. Численное поведение НЕ
изменено — все прежние быстрые ворота зелёные; эталонные отчёты и их хеши не
затронуты.

### Добавлено
- **`codemeta.json`** — машиночитаемые метаданные (схема CodeMeta/schema.org,
  `SoftwareSourceCode`); зеркалят `CITATION.cff`/`.zenodo.json`.
- **Ворота синхронизации версии** (`tests/test_version_sync.py`): единый
  источник — `plate_solver.__version__`; `CITATION.cff`, `.zenodo.json` и
  `codemeta.json` обязаны объявлять ту же версию (устраняет исторический дрейф
  витрин 0.3.1 ↔ 0.6.x перед архивацией).

### Исправлено
- **Метаданные цитирования отставали на три минорных релиза.** `CITATION.cff` и
  `.zenodo.json` объявляли 0.3.1 при коде 0.6.x; версия поднята до 0.6.2,
  описание `.zenodo.json` дополнено функционалом 0.4–0.6 (единая модель теорий,
  нелинейный контакт МОР+КТН, многосвязность, штамп/две пластины, экспорт,
  конвейер воспроизводимости). Предпочитаемый `doi:` в `CITATION.cff` переведён
  на concept-DOI (резолвится на последнюю версию).
- **Документация приведена в соответствие с 0.4–0.6.** `docs/ARCHITECTURE.md`
  освежён (слои единой модели теорий и нелинейного контакта, актуальный граф
  импортов, счётчик модулей 22→38); в `docs/API.md`/`docs/ALGORITHMS.md` описан
  выбор нормировки шага МОР `gain_mode` (0.6.1); `docs/CASE_SCHEMA.md` переведён
  с устаревшего алиаса `theory=ktn` на `ktn_linear`/`ktn_full` (докстроки схемы и шаблоны `plate-solve --new`, `cli.py`).

## [0.6.1] — 2026-07-09

Патч: исправления, **ВЛИЯЮЩИЕ НА РЕЗУЛЬТАТЫ** контактных/КТН расчётов, плюс
опциональная (аддитивная) экспериментальная постановка контакта по лицевой
поверхности. Эталонный отчёт `reference_v0.6.md` НЕ затронут (`run_reference`
использует `ktn.KTNParams` и `contact.ContactMOR`, а не изменённые пути) — хеш-
ворота зелёные, регенерация не требуется. Все прежние быстрые ворота зелёные.

### Исправлено
- **Лицевой коэффициент кривизны пресета `ktn_linear` (`theory.py`).** В пресете
  зануля́лся `h_ψ²`, из-за чего производный `face_curv_coeff` вырождался в
  `−2h_*²` вместо физического `h_c²−h_*²` (§3.2) — при ν=0.3 завышен в ≈2.8 раза.
  Контакт/лицевые величины `ktn_linear` «щупали» преувеличенную лицевую
  поверхность (зона шире, пик реакции ниже). Теперь `h_ψ²` физический (в решении
  не активен без нелинейности — меняются только лицевые величины); тождество
  `c_curv` с `FaceParams`/`KTNParams` под тестом. **Числа `ktn_linear`
  меняются** (пример a=1: ρ_c 0.396→0.354, r_max 0.0586→0.067);
  `classic`/`karman`/`ktn_full` не затронуты.
- **Нормировка шага МОР при сильном мембранном ужесточении (`contact_nl.py`).**
  Секущая нормировка `gain=w_free/q0` занижает ‖G‖ вблизи контактного состояния
  (там `q_n−r→0`, оператор ≈ линеен), из-за чего при `w_lin/w_free ≫ 1` (длинные/
  растянутые пластины) эффективная β рвёт условие сжатия теоремы 4 и итерация
  РАСХОДИТСЯ (нелинейные теории). Введён выбор нормировки (см. ниже).

### Добавлено
- **`gain_mode` в `NonlinearContactMOR`/`NonlinearTwoPlateMOR`** (`contact_nl.py`):
  `"secant"` (по умолчанию — поведение v0.6.0, обратно совместимо) |
  `"linear"` — нормировка линейной податливостью `w_max_classic/q0` (верхняя
  грань ‖G‖ при неподвижной кромке ⇒ `β_eff·‖G‖ ≤ β < 2` равномерно ⇒
  гарантированная сходимость ценой большего числа итераций). Ворота в
  `tests/test_contact_ktn.py`.
- **`contact_face.py` — экспериментальная постановка контакта по ЛИЦЕВОЙ
  поверхности** (`FacePrimaryContact`, `FacePrimaryContactKTN`): лицевой прогиб
  `v=w^{h/2}` — самостоятельное поле, условие Синьорини ставится прямо на нём;
  слабая (Галёркинова) форма связи `v↔w` убирает Δw-звон реакции при сохранении
  кромочной сингулярности. Аддитивно (штатный `contact_nl` не затронут);
  исследовательский задел. Ворота — `tests/test_contact_face.py`.

## [0.6.0] — 2026-07-07

Минорный релиз: ЕДИНАЯ параметрическая модель теорий (все четыре — пресеты
одного решателя, редукции точны по построению) и НЕЛИНЕЙНЫЙ контакт методом
обобщённой реакции ПОВЕРХ полной КТН (связки нет в литературе), многосвязные
области, диагностика зоны контакта, контактные сценарии (штамп, две пластины),
конвейер воспроизводимости и экспорт. Обратно совместимо: поведение
`classic`/`karman`/`ktn_linear`/`ktn_full` не изменено, все прежние ворота
зелёные (новые слои — чисто аддитивные). Работа велась feature-ветками
N1–N8 → `main` (каждая по зелёным воротам §0).

### Добавлено
- **Единая параметрическая модель теорий.** `theory.py` (`TheoryParams` —
  управляемые члены: `membrane`, `h_psi_sq`/`h_star_sq`, `compression`,
  `shear_field`; пресеты `classic`/`karman`/`ktn_linear`/`ktn_full`; морфинг
  `with_refinement_scale(α)`) и `ktn_solver.py` (`KTNSolver` — ОДИН решатель,
  настраиваемый пресетом; `karman` = `KarmanPlate`, `ktn_full` = `KTNPlate`
  машинно). Редукции точны ПО ПОСТРОЕНИЮ (тот же путь исполнения) — усиление
  теоремы T6.
- **Нелинейный контакт МОР+КТН.** `contact_nl.py` (`NonlinearContactMOR`): метод
  обобщённой реакции вокруг полного нелинейного `KTNSolver`; лицевое условие
  Синьорини `u_c = w + (h_c²−h_*²)Δw ≤ z` (кривизна масштабируется теорией:
  0 для classic/karman ⇒ контакт по срединной, физическая для ktn_full).
  Схемы: `nested` (эталон корректности) и `merged` (совмещённая, рабочая,
  быстрая — теорема T7, композиция сжатий МОР T4 и Пикара T5). Гейты R1/R3/R4.
- **Многосвязные области.** `geometry.make_plate_with_hole` (прямоугольник с
  круглым вырезом, `r_diff`); нелинейные теории на многосвязных `annulus`/
  `compose` (снято прежнее ограничение геометрии для karman/ktn_full).
- **Диагностика зоны контакта.** `diagnostics.py` (`contact_components` — число
  связных пятен через граф близости + union–find; `contact_report` — размер,
  доля площади, пик и локализация, суммарная сила `∫r dA`, топология);
  `NonlinearContactResult.n_components`; отрисовка профиля реакции и лицевых
  напряжений в зоне (ноутбук 07).
- **Контактные сценарии (§9.2).** Штамп: неплоское препятствие `z(x,y)` через
  поле `gap` (локализует зону под индентором). Две пластины:
  `contact_nl.NonlinearTwoPlateMOR` — взаимный контакт двух `KTNSolver`, общая
  реакция `f₁−r`/`f₂+r` (3-й закон Ньютона), непроникание лицевых
  `u_c1−u_c2≤z`; редукция к одностороннему контакту при жёсткой 2-й пластине.
- **Конвейер воспроизводимости (§9.1).** `scripts/reproduce_all.py` — «одна
  команда → все результаты»: реестр `результат → case → артефакт`, прогон
  `cases/ci`(+`--with-ladder`), сводка `results/reproduce/summary.md`, кэш.
- **Экспорт усилий и полей (§9.4).** `export.py` (`forces_on_grid` — моменты,
  реакция, мембранные `N`; `to_vtk` — legacy-VTK для ParaView без новых
  зависимостей).
- **Ноутбуки**: `07_theory_ladder` (решётка 2×2 одним решателем, морфинг α,
  подпись КТН в контакте, диагностика зоны, атлас), `08_stamp_two_plates`
  (штамп vs плоский зазор, взаимный контакт двух пластин + редукция).
- **docs**: THEORY §5-quater (единая модель + атлас различий теорий),
  ALGORITHMS §6 (совмещённая схема МОР+КТН + многосвязные ω-блоки), NOTES §22–24
  (теоремы T5 Пикар, T6 редукция, T7 совмещённый МОР–КТН), API (theory,
  ktn_solver, contact_nl, diagnostics, export, reproduce_all).

### Изменено
- **Эталонный отчёт** перегенерирован в НОВЫЙ `reference_v0.6.md` (числа золотой
  серии преемственны с v0.5 — новые слои аддитивны, `golden_config.py` не
  тронут; в отчёте изменилась лишь версия); SHA-256 в `tests/test_reference_hash.py`
  обновлён, `test_reference_run` переведён на v0.6. Исторические
  `reference_v0.3.md`/`v0.4.md`/`v0.5.md` заморожены и НЕ тронуты (§0).
- Схема композиции контакта по умолчанию — `merged` (`Config.contact_scheme`).
- Версия поднята до `0.6.0`.

### Задел
- Маршрутизация нелинейного контакта через диспетчер/case-файлы (`[model] theory`
  + профиль препятствия) и морфинг `refinement_scale` в схеме case-файлов —
  следующая версия (сейчас контакт/штамп/две пластины — прямым API `contact_nl`).
  Мягкий шарнир для КТН и алгебраические лицевые поправки `O(h²)` (`κ_q q`,
  `κ_r D r`) — отложены (сохранён доминирующий кривизный член). MMS для
  нелинейных/КТН/контактных операторов (§9.3) — средний блок, отложен.

## [0.5.0] — 2026-07-07

Минорный релиз: ПОЛНАЯ нелинейная теория Кармана–Тимошенко–Нагди в 2D, лицевые
величины первым классом, наведение порядка в таксономии теорий, редукционная
верификация и формализация алгоритмов. Обратно совместимо: поведение
`classic`/`karman`/`ktn_linear` не изменено (регресс-числа не сдвинуты). Работа
велась feature-ветками N0–N4 → `main` (каждая по зелёным воротам §0).

### Добавлено
- **Полная нелинейная КТН (`theory = "ktn_full"`).** `ktn_full.py` (`KTNPlate` —
  слой поверх `KarmanPlate`): во внеплоскостной шаг Пикара добавлены члены
  (A) `−h_*²Δq_n` и (B) регуляризация связи `+h_ψ²∫L Δv` (слабая форма Грина,
  защемление §3.5); несимметричная система — LU с диаг. предобусловливанием.
  Мягкий шарнир и Ньютон — задел (`NotImplementedError`).
- **Лицевые величины первым классом.** `faces.py` (`FaceParams`, `face_stresses`):
  прогиб лицевой `u_c`, напряжения граней (изгиб+обжатие+мембрана), интроспекция
  параметров толщины (`h_psi_sq`/`h_star_sq`/`h_c_sq`, канонические имена §3.2);
  `Result.thickness_params`, экспорт/отрисовка для любой теории.
- **Таксономия теорий.** `THEORIES = (classic, karman, ktn_linear, ktn_full)`;
  `ktn_linear` — прежнее `ktn` число-в-число; `ktn` — депрекация-алиас на
  `ktn_linear` (`DeprecationWarning`, удаление v1.0.0); `docs/MIGRATION.md`.
- **Неравномерные нагрузки.** Тип `gaussian` (аналитическая Δq) — проявляется
  член КТН (A): под неравномерной нагрузкой отличается и срединный прогиб.
- **Редукционная верификация** `tests/test_ktn_full.py` (кандидат в теорему T6):
  Gate R1 (КТН→Карман, машинная точность), R2 (→Кирхгоф), R4 (гашение O(h²/L²)),
  R5 (смыкание с ktn_linear); подпись КТН. Gate R3 (1D-КТН↔2D) отложен (§14,
  нужен 1D-эталон школы). Case-файлы ci + ladder.
- **`docs/ALGORITHMS.md`** (ГОСТ 19.701/Mermaid): псевдокод и блок-схемы ключевых
  алгоритмов (RFM-структура, Пикар+Андерсон, члены КТН, лицевые, МОР).
- Ноутбук `06_theory_comparison` обновлён под ktn_full; docs THEORY/NOTES(T6)/
  CASE_SCHEMA/API/CLAUDE.

### Изменено
- **Эталонный отчёт** перегенерирован в НОВЫЙ `reference_v0.5.md` (лестница
  включает нелинейные ступени Кармана и полной КТН `ktn_full_thickness_sweep`);
  SHA-256 в `tests/test_reference_hash.py` обновлён, `test_reference_run`
  переведён на v0.5. Исторические `reference_v0.3.md`/`reference_v0.4.md`
  заморожены и НЕ тронуты (§0); `golden_config.py` не трогался.
- Версия поднята до `0.5.0`.

## [0.4.0] — 2026-07-07

Минорный релиз: геометрически-**нелинейный** решатель по теории Кармана
(Föppl–von Kármán) и выбор теории настройкой пакета. Обратно совместимо:
поведение `classic`/`ktn` НЕ изменено (регресс-числа не сдвинуты), лестница
моделей — одним ключом `[model] theory`. Развитие в ветке `dev-postfreeze`.

### Добавлено
- **Теория Кармана (`theory = "karman"`).** `src/plate_solver/membrane.py`
  (`KarmanPlate`, `KarmanResult`): система Фёппля–Кармана в перемещениях
  (мембранная связь `L(Φ, w)`), итерация Пикара по замороженным усилиям `N_k`
  с симметричной геометрической жёсткостью `∫ N_αβ ψ_{i,α} ψ_{j,β}`, шагами по
  нагрузке и ускорением Андерсона. Канонические области (круг, квадрат),
  закрепление кромки в плане `inplane_bc ∈ {immovable, movable}`.
- **Разграничение теорий.** `THEORIES = (classic, karman, ktn)`; новый ключ
  схемы `[model] inplane_bc` и параметры нелинейной итерации (`n_load_steps`,
  `karman_relax`, `karman_max_iter`, `karman_tol`, `karman_method`) в
  `[model]`/`Config`. CLI: `--theory` / `--inplane-bc`. Значение `ktn_full`
  (полная нелинейная КТН) зарезервировано → `NotImplementedError` (релиз
  v0.5.0). Честные термины теорий задокументированы (THEORY.md, CASE_SCHEMA.md).
- **Комплекс независимых эталонов.** `src/plate_solver/benchmarks.py` —
  Hencky (мембрана), Way (ряды), Тимошенко (Ритц), Levy/IGA (ряды Фурье),
  нормировки прил. B — чистыми формулами/замороженными таблицами с
  источниками; единый источник чисел для тестов и ноутбука.
- **Верификация.** `tests/test_karman.py`: Gate L (линейный предел < 0.5 %),
  Gate M (мембранный предел Hencky, < 3 % при P̄=10³), Gate K (Way/Тимошенко/
  Levy, < 2–3 %), Gate B (immovable vs movable), воспроизводимые модульные
  тесты. Быстрые `cases/ci/karman_*.toml` (авто-CI) и тяжёлые
  `cases/ladder/karman_*.toml` (Hencky, Levy; маркер `big`).
- **Ноутбук** `notebooks/06_theory_comparison.ipynb`: три кривые
  «нагрузка–прогиб», наложенные точки эталонов и асимптота Hencky, поле
  мембранного усилия, подпись КТН в лицевых величинах.
- Мат. обоснование: эскиз теоремы T5 (сходимость нелинейной итерации),
  аргумент валидации эталонами разной природы (NOTES.md §22).

### Изменено
- **Эталонный отчёт** перегенерирован в НОВЫЙ файл `reference_v0.4.md`
  (обоснование: лестница `cases/ladder` теперь включает детерминированные
  нелинейные karman-ступени — Hencky и Levy, `reference="none"`). Строка
  SHA-256 в `tests/test_reference_hash.py` обновлена, `tests/test_reference_run.py`
  переведён на v0.4. Исторический `reference_v0.3.md` заморожен и НЕ тронут
  (протокол §0); `golden_config.py` не трогался.
- Версия поднята до `0.4.0` (единый источник `plate_solver.__version__`).

## [0.3.1] — 2026-07-06

Патч по правкам акта приёмки: документация, комментарии, ноутбуки,
эргономика вывода. Пре-тег прогон: `pytest` (все маркеры, включая
повторный эталонный прогон) — **317 passed** за ~18 мин (macOS/arm64);
лёгкая связка — 268 тестов. Поведение решателей не изменено, эталонный отчёт
reference_v0.3.md не тронут (хеш-ворота зелёные). Узкие дополнения API
ВЫВОДА (`solve(grid_n=…)`, `Result.regrid`,
`Problem.with_discretization`) включены в патч решением автора.

### Добавлено
- Сетка вывода задаётся «на месте» и уплотняется мгновенно:
  `grid_n` в шаблонах `--new` с пояснением («на точность решения не
  влияет»); CLI `plate-solve … --grid N`; программно
  `solve(problem, grid_n=…)` и `Problem.with_discretization(p, Q,
  grid_n)`; `Result.regrid(N)` — переоценка всех полей (w, моменты/σ,
  поверхности, реакция, зона) из удержанных коэффициентов и узловой
  реакции БЕЗ повторного МОР. Ворота: тождество на родной сетке
  побайтово; контакт на 4·n₀ без новых итераций.
- Копируемые примеры фабрики эталонов в docs/API.md (кольцо со
  смешанными кромками и сила в центре; Леви со свободными кромками;
  замкнутый контакт круг+основание) — исполняются тестом
  `tests/test_api_examples.py`.

### Исправлено
- Число ворот лёгкой связки в примечании к 0.3.0: 261 тест (а не 117).
- Матрица возможностей идемпотентна (FEATURES.md исключён из пула
  само-описания; вскрытая этим дыра `--help` закрыта в README).

### Изменено
- Финальная зачистка меток внутренних планов (`F#`, `P#.#`) из
  комментариев кейсов и докстрингов; стоп-словарь doc_policy расширен
  этими паттернами (ноутбуки проверяются по source-ячейкам).
- Поставка на ревью — штатный `git archive` (процессный скрипт сборки
  архива удалён из публичного репозитория; решение автора).
- Документированы grid-зависимые диагностики (топология зоны — по сетке
  вывода; в baselines.json к топологии добавлен `grid_n`).
- Метаданные релиза: имя автора унифицировано (латиница), добавлены
  ORCID и аффилиация в `.zenodo.json` и `CITATION.cff` (подготовка к
  архивации Zenodo). Код и поведение не изменены.
- Архивация на Zenodo: concept DOI 10.5281/zenodo.21218627,
  version DOI 10.5281/zenodo.21218628; бейдж и метаданные цитирования
  добавлены. Код не изменён.

## [0.3.0] — 2026-07-06

Доведение до самостоятельного программного продукта (feature-freeze:
исправления, документация, упаковка и три санкционированных дополнения:
фабрика аналитических эталонов, поля лицевых поверхностей, свободный край).

Регресс: **310 тест-ворот** (лёгкая связка `-m "not big and not fem"` —
~70 с; покрытие кода лёгкой связкой — 85 %, информационно); единый эталонный отчёт `results/reference/reference_v0.3.md`
(48 чисел в csv) под SHA-256-воротами; ключевые числа: круг p-свип
(ошибка < 0.1 %, модельный разрыв 26.42 %), L-форма RFM↔Marcus 2.64 %,
контакт МОР r_max = 112.58 (67/10800 узлов), КТН пик ×0.095 / узлы ×7.75,
1D-штамп ↔ Maple 2.4 % L², 24 ladder-ступени в допусках «факт × 3».

Пре-тег прогон: `pytest` (все маркеры big+fem, включая повторный
эталонный прогон run_reference) — **310 passed** за ~17 мин
(macOS/arm64, Python 3.14); лёгкая связка `-m "not big and not fem"` —
**261 тест** за ~70–75 с (в v0.3.0).

### Добавлено
- ФАБРИКА аналитических эталонов с самосертификацией
  (`analytic_auto`): осесимметрия (полином q(r), сила в центре, кромки
  clamped|soft|true_ss), Навье uniform|patch|point, Леви с
  несимметричными и СВОБОДНЫМИ кромками, 1D-полоса; замкнутый контакт
  круг+основание (кольцевая реакция Кирхгофа; предел согласован с
  функцией Грина). Эталона без проверенного сертификата не существует.
- СВОБОДНЫЙ КРАЙ: `type = "free"` в `[[bc.sides]]` (естественные условия
  полной формы), правило жёстких смещений, эталоны Леви-free и
  Морли-МКЭ, ступени rect_sfsf / rect_cfff (консоль).
- ПОЛЯ ЛИЦЕВЫХ ПОВЕРХНОСТЕЙ: w_top / w_bot / dh в fields.npz (схема 2,
  NOTES §21), `--surface mid|top|bottom`, σ-шестёрка ВТОРОЙ пластины
  пары; NOTES §21: канон — кинематика КТН (решение автора, §21.1),
  классическое 3D-восстановление — независимая диагностика (§21.2,
  кривизный блок тождественен, разности q,r-членов — постоянные
  sympy-ворота; различие в пределах O(h²)-точности теории).
- Единый эталонный регресс: `scripts/run_reference.py` (золотая серия +
  1D-штамп + структура-против-штрафа + вся лестница) → отчёт под
  хеш-воротами; детерминизм повторного прогона — тестом.
- CLI: `--check` (валидация для CI), `--report` (одностраничный md),
  `--version`, `--surface`; примеры в `--help`.
- Документация: THEORY.md, ARCHITECTURE.md (граф импортов), API.md,
  FEATURES.md (автоматическая матрица возможностей БЕЗ дыр), cases/README;
  CASE_SCHEMA добит (exit-коды, оси/поверхности, покрытие analytic).
- Планшет контакта пары пластин с панелями w₁/w₂
  (`viz.plot_pair_summary`; штатный путь фигур выбирает его сам).
- `scripts/make_review_zip.sh` — приёмо-архив одной командой
  (исходники + лёгкий снимок полей + свежий pytest-лог).
- Упаковка: sdist+wheel (`python -m build`, twine-чистые), MANIFEST,
  classifiers/urls, CITATION.cff 0.3.0, .zenodo.json, RELEASING.md,
  CI-job build; единый источник версии — `plate_solver.__version__`.
- CHANGELOG.md (этот файл); журнал разработки перенесён в приватную часть.

### Исправлено
- Тест редукции формулы (11) — полный вид по первоисточнику:
  мембранный член T/h, изгибный и b-поправка — h̊², знак b-поправки
  противоположен изгибному.
- `result.json` — строгий JSON: метрики, не определённые на пустой
  контактной зоне, пишутся `null` (вместо невалидного `NaN`);
  блок контакта присутствует при `contact.enabled` всегда, включая
  случай «касания нет».
- Кросс-платформенный допуск регресса профиля σy⁻ под штампом
  (чувствительность недосошедшего МОР к BLAS-платформе).
- Кросс-платформенные ворота профиля dh (канон 21.1): dh — разность
  близких величин, у кромки зоны недосошедший ci-МОР платформозависим
  вплоть до знака малых точек; гейтятся пик, глубокая зона (|dh| ≥ 0.3
  пика) и масштаб кромочного звона (hotfix по красному CI до публикации
  тега).

### Изменено
- Документация переведена на язык продукта (без внутренних процессных
  привязок); научная библиография сохранена: Михайловский–Тарасов (1993),
  Рвачёв, Ермоленко–Туркова (2023) — источник формулы (11),
  Ермоленко–Дуркин (2022) — источник 1D-эталона Maple.
- Исторический «золотой» отчёт ПОГЛОЩЁН единым reference: числа
  воспроизведены теми же функциями и сверены воротами преемственности
  (все 78 расчётных чисел присутствуют побайтово в напечатанной
  точности); legacy-файлы выведены из отслеживаемой части, хеш-ворота
  перенесены на reference_v0.3.md. Пакет-фасад `plate_solver.__init__`
  экспортирует публичные точки секциями (лениво).

## [0.2.0] — 2026-07-04

Слой постановки задачи: комплексом пользуются case-файлом, а не
программированием.

- Схема case-файлов TOML (docs/CASE_SCHEMA.md): geometry / bc / load /
  model / contact / discretization / verify / output; валидатор с
  диагностикой `CaseError` («ключ: получено X, ожидалось Y»).
- Реестр геометрий: circle, rectangle, L, annulus, мини-язык compose
  (union/intersect/difference, глубина ≤ 3, ≤ 7 узлов).
- Диспетчер `solve(problem) → Result`; result.json с провенансом
  (git-хеш, версии зависимостей).
- CLI: plate-solve / plate-verify / plate-ladder, свипы `--sweep p=A:B:S`.
- Верификация как свойство постановки: `[verify]` с эталонами
  analytic / mms / fem / cross_1d; реестр cases/ci = CI-ворота.
- Эталоны: кольцо (аналитика 4×4 и 1D-Ритц), точечная сила
  (регуляризованное пятно, NOTES §18), fem (scikit-fem, Аргирис/Маркус).
- Золотой отчёт заморожен хеш-воротами (SHA-256).
- 210 тест-ворот.

## [0.1.0] — 2026-07-02

Первый рабочий выпуск: изгиб и односторонний контакт упругих пластин
произвольного очертания.

- Геометрия R-функциями (система R0 Рвачёва), структуры решения ω·Φ
  (мягкий шарнир) и ω²Φ (защемление) на базисе Чебышёва.
- Изгиб: расщепление бигармоники на две задачи Пуассона (мягкий шарнир);
  прямой Ритц по (D/2)∫(Δw)² (защемление).
- Контакт: метод обобщённой реакции r ← [r + β(w − Δ)]₊ с теоремой
  сходимости; неканонические области (L-форма с входящим углом).
- Поправки уточнённой теории КТН (сдвиг + обжатие) в изгибе и контакте.
- 1D-задел: балка-полоса, штамп (Ритц и функция Грина), эталон Maple.
- Верификационная лестница 1D↔2D↔аналитика; золотой прогон одной серией
  (scripts/run_golden.py).
- 104 тест-ворот.
