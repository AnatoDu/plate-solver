"""plates — рабочий 2D-комплекс для расчёта пластин произвольного очертания.

Метод: R-функции (геометрия + краевые условия) + метод Ритца (решатель на базисе
``ω·Φ``, Φ — полиномы Чебышёва) + метод обобщённой реакции (контакт). Изгиб
считается расщеплением бигармоники ``D·Δ²w = q̃`` на две задачи Пуассона с одним
оператором ``−Δ`` и однородным Дирихле — см. ``plates.plate``.

Зависимость на ``plate_solver`` односторонняя: отсюда переиспользуются готовые
R-функции, аналитика круглой пластины и цилиндрическая жёсткость.

Подмодули импортируются лениво (по требованию), чтобы ``import plates`` и доступ к
``plates.config`` не тянули за собой numpy/sympy/plate_solver раньше времени.
"""

from __future__ import annotations

from . import config
from .config import Config

__version__ = "0.0.1"

__all__ = ["config", "Config", "__version__"]
