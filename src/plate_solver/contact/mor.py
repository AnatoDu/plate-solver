"""Метод обобщённой реакции (МОР) для контакта со свободной границей (заготовка).

Схема итерации (наполняется по главе 3):
  1. Решить задачу изгиба при текущем распределении реакции основания.
  2. Проверить одностороннюю связь (комплементарность):
        прогиб − зазор <= 0,   реакция >= 0,   реакция·(прогиб − зазор) = 0.
  3. Обновить обобщённую реакцию там, где связь нарушена.
  4. Повторять до сходимости.
Сходимость на канонических областях доказана (Михайловский–Тарасов, ПММ 1993);
перенос/обобщение на произвольную область и оценки — теоремы главы 3.
"""

from __future__ import annotations

from ..model.problems import ContactProblem


def generalized_reaction(
    problem: ContactProblem,
    grid,
    tol: float = 1e-6,
    max_iter: int = 200,
):
    r"""Решить контактную задачу методом обобщённой реакции.

    Returns
    -------
    Поле прогиба, распределение контактной реакции и зона контакта
    (в т. ч. её топология). ЗАГОТОВКА: пока не реализовано.
    """
    raise NotImplementedError(
        "Метод обобщённой реакции на произвольной области — глава 3 (МОР + теоремы)."
    )
