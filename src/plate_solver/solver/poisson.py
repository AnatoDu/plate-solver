"""Решение задачи Пуассона на произвольной области (заготовка).

Опирается на задел группы (Ермоленко–Поздеев). Бигармонику удобно сводить
к последовательности задач Пуассона, поэтому этот решатель — базовый кирпич.
"""

from __future__ import annotations


def solve_poisson(omega, rhs, grid, bc="dirichlet"):
    r"""Решить −Δu = rhs на области {ω > 0} с заданными краевыми условиями.

    Parameters
    ----------
    omega : R-функция области (geometry.rfunctions).
    rhs : правая часть f(x, y).
    grid : сетка/дискретизация (определяется в главе 2).
    bc : тип краевого условия ('dirichlet' и т. д.).

    Returns
    -------
    Поле решения на узлах. ЗАГОТОВКА: пока не реализовано.
    """
    raise NotImplementedError(
        "Решатель Пуассона на произвольной области — глава 2 (метод + теоремы)."
    )
