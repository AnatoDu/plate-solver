"""Решение бигармонического уравнения изгиба на произвольной области (заготовка).

Опирается на задел группы (Ермоленко–Кожагельдиев). Классический изгиб:
    D ΔΔw = q  на {ω > 0},  + краевые условия на ω = 0.
Уточнённая теория (КТН) даёт связанную систему — наполняется по главе 1–2.
"""

from __future__ import annotations

from ..model.ktn import PlateMaterial


def solve_biharmonic(omega, load, grid, material: PlateMaterial, edge="clamped"):
    r"""Решить ``D ΔΔw = load`` на {ω > 0} с заданным закреплением края.

    Returns
    -------
    Поле прогиба w на узлах сетки. ЗАГОТОВКА: пока не реализовано.
    """
    raise NotImplementedError(
        "Решатель бигармоники на произвольной области — глава 2 (метод + теоремы)."
    )


def solve_clamped_circular(a: float, q: float, material: PlateMaterial, n: int = 64):
    r"""Численно решить защемлённую круглую пластину как ДВУМЕРНУЮ задачу.

    Это точка входа для верификации 1D ↔ 2D: тот же случай, что и
    ``analytic.circular.clamped_uniform``, но решённый общим 2D-методом на
    круге, заданном R-функцией. Когда метод будет реализован, автотест
    ``tests/test_verification_1d_2d.py`` начнёт сравнивать 2D-решение с
    аналитикой и станет рабочим критерием корректности.

    Parameters
    ----------
    a : радиус пластины.
    q : равномерная поперечная нагрузка.
    material : материал (даёт жёсткость D).
    n : параметр дискретизации.

    Returns
    -------
    callable w(x, y) или поле прогиба на сетке (интерфейс уточняется в главе 2).
    ЗАГОТОВКА: пока не реализовано.
    """
    raise NotImplementedError(
        "2D-решение круглой пластины ещё не реализовано — нужно для верификации 1D↔2D."
    )
